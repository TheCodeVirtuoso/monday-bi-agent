# --- LLM providers ---
# openai covers Groq and OpenRouter (both OpenAI-compatible endpoints);
# anthropic is the optional paid fallback.
openai>=1.40.0
anthropic>=1.0.0

# --- web ---
# Two front ends over one orchestrator: FastAPI + static chat UI (app.py,
# used on Render/Railway/Docker) and Gradio (gradio_app.py, used on Hugging
# Face Spaces, which does not offer Docker on the free tier).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gradio>=5.0.0
httpx>=0.27.0
pydantic>=2.7.0

# --- data ---
pandas>=2.2.0
# Required by pandas.read_excel for .xlsx — the FileBackend fails without it.
openpyxl>=3.1.0
python-dotenv>=1.0.0
