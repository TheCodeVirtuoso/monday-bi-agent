# --- LLM providers ---
# openai covers Groq and OpenRouter (both OpenAI-compatible endpoints);
# anthropic is the optional paid fallback.
openai>=1.40.0
anthropic>=1.0.0

# --- web ---
# The deployed front end is FastAPI + the static chat UI in static/.
# `gradio_app.py` is an alternative front end over the same orchestrator; its
# dependency lives in requirements-dev.txt rather than here, because Gradio
# pulls a large tree and the target free tier has 512 MB of RAM to spend on
# pandas and the app itself.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.7.0

# --- data ---
pandas>=2.2.0
# Required by pandas.read_excel for .xlsx — the FileBackend fails without it.
openpyxl>=3.1.0
python-dotenv>=1.0.0
